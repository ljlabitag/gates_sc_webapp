node_modules
.DS_Store
dist

# Secrets — never commit. Set via `wrangler secret put` instead.
.env
.env.*

# Build metadata
*.tsbuildinfo
.vite

# Wrangler local dev state (simulated D1/R2 data, telemetry)
.wrangler
