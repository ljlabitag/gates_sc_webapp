DATABASE_URL="postgresql://USER@localhost:5432/gates_conference?schema=public"

PORT=4000
CLIENT_ORIGIN="http://localhost:5173"

ADMIN_USER="admin"
ADMIN_PASSWORD="change-me"

# Email is stubbed (logged to console) until SMTP_HOST is set — see src/lib/mailer.ts
SMTP_HOST=""
SMTP_PORT="587"
SMTP_USER=""
SMTP_PASS=""
SMTP_FROM="GATES Program <no-reply@gates-program.org>"
